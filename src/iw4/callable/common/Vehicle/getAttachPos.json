{
	"usage": "<entity> getAttachPos(<node>)",
	"description": [
		"Returns the origin and angles if the vehicle were to be attached to the path. The origin and angles are returned as a vector array of size 2. Origin is 1st and angles is 2nd.",
		"*🚩 This documentation is for the IW3 version of this function credit to Raid Gaming and has yet to be reviewed/updated for IW4.*"
	],
	"params": [
		{
			"name": "node",
			"types": ["Entity"],
			"description": ["A node on the path to attach."],
			"optional": false
		}
	],
	"return": {
		"types": ["Array"]
	},
	"example": ["pos = self getAttachPos(node);\norigin = pos[0];\nangles = pos[1];"]
}
