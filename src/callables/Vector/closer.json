[
	{
		"scopes": ["iw3|sp-mp"],
		"usage": "closer(<reference>, <point A>, <point B>)",
		"description": [
			"Tests which of two points is the closest. Returns true if point A is closer to the reference than point B."
		],
		"params": [
			{ "name": "reference", "types": ["Vector3"], "description": ["The base position."] },
			{ "name": "point A", "types": ["Vector3"], "description": ["The first point."] },
			{ "name": "point B", "types": ["Vector3"], "description": ["The second point."] }
		],
		"return": { "types": ["bool"] },
		"example": ["if (closer(center, models[j].origin, models[i].origin)) {\n  // Code\n}"]
	},
	{
		"scopes": ["iw4|sp-mp"],
		"description": ["Get if vector `a` is closer to a reference point than vector `b`."],
		"params": [
			{ "name": "reference", "types": ["vector"] },
			{ "name": "a", "types": ["vector"] },
			{ "name": "b", "types": ["vector"] }
		],
		"return": {
			"types": ["bool"],
			"description": ["Whether `a` is closer to `reference` than `b`."]
		}
	}
]
